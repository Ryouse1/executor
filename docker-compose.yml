version: "3.9"
services:
  web-ide:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
    environment:
      NODE_ENV: production
    restart: unless-stopped
